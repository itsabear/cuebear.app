# Cue Bear v1.0.9 Changelog

## Bug Fixes

### 1. USB chip visibility fix
- USB chip now only appears when Bridge app is running (not just when cable is connected)
- Modified `ConnectionManager.swift` to check `connectedComputerName != nil` instead of `isUSBCableConnected`

### 2. USB chip timing fix
- USB chip now appears immediately when connection is established (synchronized with "Connected" status)
- Added `checkUSBBridgeAvailability()` call immediately after handshake completes in `ConnectionManager.swift:678`

### 3. Song library + icon behavior
- Plus icon now stays visible but turns grey when song is already in cue list (instead of disappearing)
- Modified `ControlSubviews.swift:381-404` to show greyed-out icon with `.disabled()` state

### 4. Button preview sizing
- Button previews in create/edit mode now match the actual button sizes in control area
- Changed preview dimensions in `ContentView.swift:1388-1427`:
  - Regular buttons: 156×72 (matching 2.17:1 aspect ratio)
  - Small buttons: 72×72 (1:1 ratio)

### 5. Dark mode support
- Comprehensive dark mode implementation across all views
- Changes in multiple files:
  - `Components.swift`: Transport controls, shadows
  - `ControlSubviews.swift`: Performance rows, badges
  - `ContentView.swift`: Control buttons, faders, delete icons
  - `TransportPreviews.swift`: Preview components
- Replaced hardcoded `.white` with `Color(uiColor: .systemBackground)`
- Replaced hardcoded `.black` with `Color.primary`
- Button icons explicitly use `.accentColor` to maintain blue in all modes
- All shadows use `Color.primary.opacity()` instead of `.black.opacity()`

### 6. Delete button improvements
- Delete X buttons in edit mode are now easier to tap:
  - Circle size: 22×22 → **30×30** (36% larger)
  - Icon size: 10pt/12pt → **13pt**
  - Hit target: 60×60 → **70×70**
  - Opacity: 0.85 → **0.9**
  - Better positioning: Moved further to top-left corner
- Changes in `ContentView.swift`:
  - Regular buttons: offset (-25, -25) → **(-32, -32)**
  - Faders: offset (-25, -25) → **(-32, -32)**
  - iPad tiles: offset (-23, -23) → **(-30, -30)**

### 7. Save & Add Another MIDI type preservation
- When creating a Note control and clicking "Save & Add Another", the next control now correctly defaults to Note (instead of always resetting to CC)
- Fixed in `ControlSubviews.swift:1154-1177`:
  - Now preserves `savedKind` before resetting form
  - Sets `kind = savedKind` to maintain MIDI type
  - Calls `autoAssignMIDI(for: savedKind, ...)` with preserved type
- Note: Control buttons/faders were already working correctly

## New Features

### 8. Smart auto-assign with channel increment
- **Without global mode**: When reaching CC/note 127, automatically moves to next channel and continues from 0
  - Allows up to **2,032 controls** (127 × 16 channels) instead of just 127
- **With global mode**: Shows error popup when limit reached
  - Message: "You've reached the maximum of 127 controls in Global MIDI Channel mode. Turn off Global MIDI Channel mode to continue auto-assigning on additional channels."
- Applies to all creation methods:
  - +Button
  - +Fader
  - +Cue list
  - Save & Add Another
- Implementation:
  - Created new `autoAssignMIDI()` function in `ContentView.swift:916-981` and `ControlSubviews.swift:1011-1066`
  - Added alert states in both files
  - Updated all `.onChange` handlers and preset functions

## UI Improvements

### 9. Projects menu reordering
- "New Project" button now appears first in the Actions section, above "Save"
- Modified `ControlSubviews.swift:781-788` (CBProjectsSheet)
- Improves workflow by prioritizing new project creation

### 10. Edit mode tap behavior for cue list and song library
- **Normal mode (not editing)**:
  - Context menu removed from cue list items
  - Tapping cue list items triggers/cues the song (existing behavior)
  - Song library items can be browsed but not edited via tap
- **Edit mode**:
  - Tapping any cue list item opens the edit sheet
  - Tapping any song library item opens the edit sheet
  - Context menu removed (no long-press menu)
- Implementation in `ControlSubviews.swift`:
  - Added `isEditing: Bool` parameter to `CBPerformanceList` (line 9)
  - Added `isEditing: Bool` parameter to `CBPerformanceRow` (line 57)
  - Modified tap gesture behavior (lines 79-96) to call `onRename()` in edit mode
  - Changed from `.simultaneousGesture` to `.gesture` (line 149) to give tap handler priority
  - Removed context menu entirely (line 150)
  - Updated song library tap logic (line 413): `onTap: (!isEditing || batchMode) ? nil : { onRename(row.song) }`
- Modified `ContentView.swift:1871` to pass `isEditing` state to `CBPerformanceList`

### 11. Preserve unsaved changes in cue edit sheet
- Cue list edit behavior now matches button/fader edit behavior
- When you edit a cue and dismiss the sheet (tap outside), your unsaved changes are preserved
- Reopening the same cue shows your previous edits, allowing you to continue editing
- Changes are cleared when you Save or Delete (fresh start for next edit)
- Implementation in `ControlSubviews.swift` (CBAddEditCueSheet):
  - Added `lastEditingSongID` state variable to track which cue is being edited (line 876)
  - Modified `.onAppear` to only preset form if editing a different cue (lines 964-971)
  - Modified `.onChange(of: editingSong)` to only preset when cue ID changes (lines 972-980)
  - Clear `lastEditingSongID` after Save (line 1172) and Delete (line 945)

### 12. Enhanced cue list tap feedback
- Made successful cue tap feedback more noticeable and snappier for better user experience
- Changes in `ControlSubviews.swift` (CBPerformanceRow):
  - Flash now works in both regular mode (triggering MIDI) and cue mode (cueing songs)
  - Haptic feedback upgraded from `.light` to `.medium`
  - Flash animation duration optimized to 0.20s - noticeable but quick (line 167)
  - Feedback now triggers in cue mode when cueing a new song (previously only in regular mode)
  - Press-down animation response time reduced from 0.18s to 0.10s for snappier feel (line 151)
  - Flash and haptic now trigger immediately on finger release via `.onChange(of: isPressed)` instead of `.onEnded` for instant visual feedback with no delay (lines 153-159)
  - When dragging (scrolling), no flash occurs - just smooth "safe unpress" animation back to full size

### 13. Native swipe gestures replace edit mode buttons
- **Complete redesign** of cue list and song library editing to use native SwiftUI swipe gestures instead of problematic +/- buttons
- **Cue List changes**:
  - Removed red minus (-) buttons
  - Added swipe-left-to-delete (supports full swipe for quick deletion)
  - Swipe delete only available when not in reorder mode
  - Added "Reorder" mode button in header (replaces need for always-visible drag handles)
  - Drag handles only appear in reorder mode
  - Cue list is undraggable when not in reorder mode
- **Song Library changes**:
  - Removed blue plus (+) buttons from normal mode
  - Added swipe-right-to-add for adding songs to cue list
  - Added swipe-left-to-delete for removing songs from library
  - Swipe actions disabled in batch select mode
  - Songs already in cue list show with 0.55 opacity and can't be added again
  - Selection radio buttons appear in batch mode (circle with checkmark when selected)
- **Data integrity**:
  - Songs deleted from library are automatically removed from cue list
  - Songs deleted from cue list are automatically ungreyed in library (opacity restored to 1.0)
- Implementation in `ControlSubviews.swift`:
  - **CBSetlistRow** (lines 165-194): Simplified row without minus button
  - **CBSetlistColumn** (lines 227-318):
    - Added `reorderMode: Binding<Bool>` parameter (line 231)
    - Added reorder button in header (lines 245-252)
    - Added swipe-left-to-delete with conditional display (lines 295-303)
    - Edit mode only active when `reorderMode && searchText.isEmpty` (line 311)
  - **CBLibraryColumn** (lines 321-445):
    - Added selection radio buttons in batch mode (lines 399-414)
    - Added swipe-right-to-add gesture (lines 438-448)
    - Added swipe-left-to-delete gesture (lines 449-457)
    - Tap behavior: batch mode toggles selection, normal mode opens rename sheet (lines 428-434)
- Modified `ContentView.swift`:
  - Added `@State private var reorderMode = false` (line 1702)
  - Passed `reorderMode: $reorderMode` to CBSetlistColumn (line 1842)

### 14. Icon-based header buttons for cleaner UI
- Replaced text-based buttons with icons for a more compact, modern interface
- **Cue List header**:
  - Reorder button: "Reorder"/"Done" → `line.3.horizontal` icon (normal) / `checkmark` icon (reorder mode)
  - Icon sized with `.frame(minWidth: 20)` to match other buttons
- **Song Library header**:
  - Sort button: Removed "A-Z" text label → Shows only `arrow.up.arrow.down` icon
  - Select button: "Select"/"Cancel" → `checkmark.circle` icon (normal) / `xmark` icon (batch mode)
- Implementation in `ControlSubviews.swift`:
  - Reorder button (lines 245-252)
  - Sort button (lines 345-352)
  - Select button (lines 354-361)

### 15. Cue list item height standardization
- Cue list items now match song library item height for visual consistency
- Changed `CBSetlistRow` padding from `.vertical(10)` to `.vertical(6)` in `ControlSubviews.swift:182`
- Creates uniform appearance across both lists in edit mode

### 16. Dark mode button compatibility
- All buttons now work properly in dark mode with adaptive system colors
- Changes in `Components.swift` (WhiteCapsuleButtonStyle):
  - Background: `Color.white` → `Color(UIColor.secondarySystemBackground)` (line 213)
  - Shadow: `.black.opacity(0.05)` → `Color.primary.opacity(0.05)` (line 215)
  - Button height standardized to `minHeight: 28` for all buttons (line 211)
- Ensures buttons maintain readability and proper contrast in both light and dark modes

### 17. Edit button height standardization
- Title bar Edit button now matches height of all other buttons in the interface
- Added `.frame(minHeight: 28)` to WhiteCapsuleButtonStyle definition (line 211)
- Standardizes height across all 19+ buttons in the app for visual consistency

### 18. Cue list flash accuracy fix
- Flash animation now only appears when MIDI is actually sent (not on unsuccessful taps)
- Flash correctly suppressed when:
  - In cue mode (just selecting, not triggering)
  - Scrolling/dragging the list
  - In edit mode (editing, not triggering)
- Implementation in `ControlSubviews.swift` (CBPerformanceRow):
  - Removed `.onChange(of: isPressed)` flash trigger (prevented premature flash)
  - Moved flash to `.onEnded` handler with `!isCueMode` condition (lines 79-101)
  - Added `didDrag` check with 50ms delay before reset to prevent race conditions
  - Flash only triggers on successful tap without drag in regular mode

### 19. Song library batch controls redesign
- **Complete UX overhaul**: Batch controls now replace search bar when in batch mode
- **Text-only CTAs** (clean, native iOS style):
  - `Select All` - blue text, always enabled
  - `Clear` - blue text, always enabled
  - `Add` - blue text when items selected, gray when disabled
  - `Delete` - red text when items selected, gray when disabled
- **Benefits**:
  - All batch controls right next to Select button at top (no reaching to bottom)
  - Search hidden during batch mode (can't search while selecting anyway)
  - More screen space for song list
  - Matches native iOS design language (Mail, Photos, Files apps)
- Implementation in `ControlSubviews.swift`:
  - Conditional layout: batch controls when `batchMode == true`, search bar otherwise (lines 378-443)
  - Added 4 new callbacks to CBLibraryColumn: `onSelectAll`, `onClearSelection`, `onBatchAddToSetlist`, `onBatchDelete` (lines 337-340)
  - Removed bottom CBBatchToolbar overlay from `ContentView.swift` (previously lines 1852-1862)
  - Haptic feedback on Add and Delete actions

### 20. Small button preview and save fix
- **Preview fix**: Button preview now immediately updates when changing between "Button" and "Small Button" in editor
  - Added `.id("button-\(draft.isSmall)")` to force SwiftUI re-render when size changes
  - Added `.id("fader-\(draft.faderOrientation)-\(draft.faderDirection)")` for fader orientation changes
  - Changes in `ContentView.swift:641-642` (CBControlEditorSheet preview)
- **Save fix**: Small buttons now correctly appear as 1x1 in control area (not 2x1)
  - Changed `saveControl` to read from `draft.isSmall` instead of stale `buttonType` state
  - Fixed in `ContentView.swift:1048`: `b.isSmall = draft.isSmall`
  - Picker updates `draft.isSmall` directly (line 585), so save must read from draft
- **Result**: Preview and actual button now both correctly reflect small button size selection

### 21. GO button tap animation
- GO button now has tactile press feedback matching control area buttons
- Scales up to 1.05× when pressed with spring animation
- Implementation in `Components.swift` (CBTransportDock):
  - Added `@State private var isGoPressed` to track press state (line 137)
  - Added `.scaleEffect(isGoPressed ? 1.05 : 1.0)` with spring animation (lines 192-193)
  - Added `DragGesture(minimumDistance: 0)` to detect press/release (lines 194-204)
- Provides consistent tactile feedback across all interactive elements

### 22. Next song preview text truncation
- Song names in "Next:" preview chip now truncate to 19 characters with ellipsis
- Prevents long song names from making the chip too wide or wrapping
- Implementation in `Components.swift:146`: `Text("Next: \(String(name.prefix(19)))\(name.count > 19 ? "..." : "")")`
- Example: "Next: Very Long Song Name That Exceeds..." instead of full text
- Maintains clean, compact transport dock appearance

---

## Version Update

- Updated `MARKETING_VERSION` in project.pbxproj from 1.0.8 to 1.0.9
- This ensures Xcode archives the app with the correct version number

---

**Last Updated**: 2025-10-25
